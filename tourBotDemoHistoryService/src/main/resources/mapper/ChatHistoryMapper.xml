<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.tourBot.history.mapper.ChatHistoryMapper">

    <resultMap id="chatHistoryMap" type="org.tourBot.history.domain.ChatHistory">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="sessionId" column="session_id"/>
        <result property="role" column="role"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="getHistory" resultMap="chatHistoryMap">
        SELECT
            id
            , user_id
            , session_id
            , role
            , content
            , created_at
        FROM chat_history
        WHERE user_id = #{userId}
        AND session_id = #{sessionId}
        ORDER BY created_at ASC
        LIMIT 20
    </select>

    <insert id="saveHistory" parameterType="ChatHistory">
        INSERT INTO chat_history (
            user_id
            , session_id
            , role
            , content
            , created_at
        ) VALUES (
            #{userId}
            , #{sessionId}
            , #{role}
            , #{content}
            , NOW()
        )
    </insert>

</mapper>